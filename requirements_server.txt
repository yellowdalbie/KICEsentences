# 운영 서버 런타임 의존성
#
# 버전은 2026-09-03 기준 서버에 실제로 설치되어 정상 동작 중인 값으로 고정한다.
# 고정하지 않으면 서버를 새로 세우거나 재설치할 때 다른 버전이 깔려, 특히
# numpy/scipy/scikit-learn 조합이 어긋나면 원인을 찾기 어려운 문제가 생긴다.
#
# 버전을 올릴 때는 로컬에서 검증한 뒤 이 파일을 갱신할 것.

# 웹 프레임워크
flask==3.1.3
werkzeug==3.1.6

# 해설 렌더링 및 살균
Markdown==3.10.2
bleach==6.3.0

# 검색 엔진 (벡터 유사도 + BM25)
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
rank_bm25==0.2.2

# 이메일 발송(Resend API), 서버 부하 확인
requests==2.31.0
psutil==7.2.2

# 프로덕션 WSGI 서버 (현재는 미사용, 전환 대비)
gunicorn==25.1.0

# 참고: 아래 패키지는 서버에 필요 없다. 로컬 빌드 전용이므로
# requirements_build.txt 로 분리했다.
#   - sentence-transformers : 벡터 인덱스 생성 (build_vectors.py)
#   - pypdfium2             : 썸네일 생성 (generate_thumbnails_v4.py)
# pandas 는 어느 코드에서도 쓰이지 않아 제거했다.
